# 家庭记账应用 v1.3 测试报告

## 基本信息

- 测试版本：v1.3
- 测试日期：2026-03-24
- 测试人员：
- 测试环境：
  - 前端访问域名：
  - 后端地址（公网）：`https://<你的域名>`
  - 后端地址（本机）：`http://127.0.0.1:8031`
  - 部署方式：Docker Compose（backend + cloudflared）
  - 关键环境变量（如有变更请记录）：
    - `ACCOUNTING_COOKIE_DOMAIN`：
    - `ACCOUNTING_CORS_ORIGINS`：
    - `ACCOUNTING_SETUP_KEY`：

***

## 测试范围

### In（本次覆盖）

- 登录/注册（邀请码）
- 邀请码管理页（生成/查看/作废）
- Cookie 登录态 + CSRF 校验
- 自动后台同步 + “立即同步”
- 回收站（删除/恢复/清空）与跨端一致性
- Docker 部署落盘、日志滚动、备份与恢复演练
- HTTPS 访问（通过 Cloudflare Tunnel）

### Out（本次不覆盖）

- 性能压测/高并发
- 多账号/家庭成员权限模型
- 复杂冲突合并（按字段合并/手动选择）

***

## 功能测试清单（v1.3 新增与关键回归）

说明：测试结果建议使用 `通过 / 不通过 / 待测`。

### 1. 后端容器健康与落盘

| 测试项                                 | 测试结果（通过/不通过/待测） | 问题描述   |
| ----------------------------------- | --------------- | ------ |
| 容器启动成功且无反复重启                        | `通过`            | <br /> |
| 本机访问 `/health` 返回 `{"status":"ok"}` | `通过`            | <br /> |
| 数据落盘（存在 `accounting.sqlite3*`）      | `通过`            | <br /> |
| 日志落盘（`/logs/app.log` 有输出）           | `通过`            | <br /> |
| 日志按天滚动且保留 7 天                       | `通过`            | <br /> |

### 2. Cloudflare Tunnel 与 HTTPS

| 测试项                                             | 测试结果（通过/不通过/待测） | 问题描述   |
| ----------------------------------------------- | --------------- | ------ |
| 外网访问 `https://<域名>/health` 返回 `{"status":"ok"}` | `通过`            | <br /> |
| NAS 本机端口仅 `127.0.0.1` 绑定未对公网暴露                  | `通过`            | <br /> |
| 浏览器地址栏为 HTTPS（无混合内容告警）                          | `通过`            | <br /> |

### 3. CORS（仅前后端不同源时必测）

| 测试项                  | 测试结果（通过/不通过/待测） | 问题描述   |
| -------------------- | --------------- | ------ |
| 白名单域名正常调用后端接口        | 待测              | <br /> |
| 非白名单域名请求被浏览器拦截（符合预期） | 待测              | <br /> |
| 控制台无 CORS 报错         | 待测              | <br /> |

### 4. 首次注册/登录（邀请码）

| 测试项                                       | 测试结果（通过/不通过/待测） | 问题描述                             |
| ----------------------------------------- | --------------- | -------------------------------- |
| “获取初始邀请码（仅首次）”成功（仅服务端无用户时）                | `通过`            | <br />                           |
| 使用邀请码注册成功                                 | `通过`            | <br />                           |
| 注册后自动登录成功                                 | `通过`            | <br />                           |
| 登录成功后 `/auth/me` 返回用户名                    | `通过`            | <br />                           |
| 已存在用户时再次注册返回 `single_user_already_exists` | `不通过`           | 当前版本无法获取邀请码，仅首次可通过初始邀请码注册且只有一次机会 |

### 5. 登录态与 CSRF

| 测试项                                 | 测试结果（通过/不通过/待测） | 问题描述   |
| ----------------------------------- | --------------- | ------ |
| 未登录访问受保护接口返回 401（如 `/sync/version`） | `通过`            | <br /> |
| 登录后刷新页面仍保持登录态                       | `通过`            | <br /> |
| 写接口缺少 CSRF 返回 403（如 `/sync/push`）   | `通过`            | <br /> |
| 写接口带 `x-csrf-token` 后成功             | `通过`            | <br /> |

### 6. 邀请码管理页（登录后）

| 测试项                                | 测试结果（通过/不通过/待测） | 问题描述                |
| ---------------------------------- | --------------- | ------------------- |
| 设置页入口可打开 “邀请码管理” 页面                | `不通过`           | 点击邀请码管理按钮会重新调转到应用首页 |
| 可生成邀请码（默认 7 天有效）                   | `不通过`           | 无法获取邀请码             |
| 列表展示状态：active/used/expired/revoked | 待测              | <br />              |
| 可作废未使用邀请码（active -> revoked）       | 待测              | <br />              |
| 未登录不可访问邀请码管理页与接口                   | 待测              | <br />              |

### 7. 同步（增量 + 幂等 + 可观测性）

| 测试项                               | 测试结果（通过/不通过/待测） | 问题描述                |
| --------------------------------- | --------------- | ------------------- |
| 账本同步                              | 不通过             | 缺少账本同步，以及当前选择账本功能同步 |
| 设置页展示同步状态（未同步/同步中/已同步/失败）         | `通过`            | <br />              |
| 点击“立即同步”成功并显示拉取/上报/版本号            | `通过`            | <br />              |
| 连续点击“立即同步”不会产生异常（合并/退避）           | `通过`            | <br />              |
| 本地新增记录后自动后台同步（无需手动点）              | `通过`            | <br />              |
| 本地编辑记录后自动后台同步                     | `通过`            | <br />              |
| 断网新增/编辑不报错，恢复网络后自动补偿同步            | 待测              | <br />              |
| 重复上报同一批数据不会重复插入（幂等）               | 待测              | <br />              |
| 触发 `baseline_required` 时可自动拉取基线恢复 | 待测              | <br />              |

### 8. 回收站（删除/恢复/清空）与跨端一致性

| 测试项                   | 测试结果（通过/不通过/待测） | 问题描述   |
| --------------------- | --------------- | ------ |
| 删除交易记录后正常列表不可见        | `通过`            | <br /> |
| 设置页回收站可看到被删记录         | `通过`            | <br /> |
| 回收站“恢复”后记录回到正常列表      | `通过`            | <br /> |
| “彻底删除”二次确认文案正确且可清空回收站 | `通过`            | <br /> |
| 清空后不可恢复（本机）           | `通过`            | <br /> |
| 其他设备同步后同样不可见/不可恢复     | `通过`            | <br /> |

### 9. 回收站 30 天自动清理（服务端定时 + 同步传播）

| 测试项                                                 | 测试结果（通过/不通过/待测） | 问题描述   |
| --------------------------------------------------- | --------------- | ------ |
| 定时清理触发后日志包含 `auto_recycle_purge_ok`                 | 待测              | <br /> |
| 其他设备同步后回收站减少（收到清理传播）                                | 待测              | <br /> |
| 快速验证：将 `ACCOUNTING_RECYCLE_RETENTION_DAYS=0` 后可触发清理 | 待测              | <br /> |

### 10. 备份与恢复演练（运维级）

| 测试项                                 | 测试结果（通过/不通过/待测） | 问题描述   |
| ----------------------------------- | --------------- | ------ |
| 停止项目后备份 `accounting.sqlite3*` 到备份目录 | 待测              | <br /> |
| 清空数据后使用备份文件覆盖可恢复                    | 待测              | <br /> |
| 启动项目后能正常登录并同步到原数据                   | 待测              | <br /> |

***

## 体验反馈（基于实际使用填写）

### 1. 新手路径（首次注册/登录/同步）

- 体验评分（1-5）：2
- 顺畅点：首次可正常注册登陆使用
- 卡点（哪里不理解/不敢点/不知道下一步）：1.无法获取邀请码导致无法再次注册，2.同步状态缺少账本同步功能导致整体体验不佳，3.账户页面选择多账本统计数据，仅针对账户页面处理，不可影响多账本切换数据
- 建议：

### 2. 同步可感知性（状态、提示、失败恢复）

- 体验评分（1-5）：5
- 状态文案是否清楚（未同步/同步中/已同步/失败）：当前缺少账本功能

  失败时提示是否可行动（例如“重新登录/检查网络/点击立即同步”）：
- 建议：

### 3. 回收站交互（恢复/清空/二次确认）

- 体验评分（1-5）：5
- 二次确认是否足够明确（“不可恢复”的风险提示）：明确
- 建议：

***

## 问题记录

### 问题1

- **问题描述**：账户页面的多账本选择统计功能，仅针对账户页面数据，不要影响其他页面的数据显示
- **复现步骤**：
  1\.
  账户页面选择多账本时候2.账户页面可统计多个账本的数据3.切回首页数据也会变为多账本数据，切换账本功能失效

  **期结果**：
- **实际结果**：
- **严重程度**：（阻断/严重/一般/建议）
- **优先级**：（高/中/低）

***

### 问题2

- **问题描述**：缺少多账本同步功能，
- **复现步骤**：
  1\.更换登陆浏览器是，需要通过新建账本才会触发其他浏览器创建的账本信息
  2\.
  3\.
- **预期结果**：
- **实际结果**：
- **严重程度**：（阻断/严重/一般/建议）
- **优先级**：（高/中/低）

***

## 测试总结

- **通过功能数**：
- **不通过功能数**：
- **待测功能数**：
- **问题总数**：
- **总体评价**：

***

## 备注

- 若本次涉及公网部署，请记录实际的域名、Cookie 域策略（`ACCOUNTING_COOKIE_DOMAIN`）与 CORS 白名单（`ACCOUNTING_CORS_ORIGINS`），便于复现与定位问题。

