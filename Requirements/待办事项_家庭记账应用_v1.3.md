# 家庭记账应用待办事项 v1.3（后端与同步）

## 版本信息
- 版本：v1.3
- 创建日期：2026-03-16
- 关联方案：方案B（Python + SQLite）

## 待办事项列表

### 第一阶段：后端项目骨架与可部署最小闭环
- [x] **任务1：创建后端服务项目骨架**
  - 优先级：高
  - 预估工作量：0.5-1天
  - 依赖关系：无
  - 验收标准：
    - 后端目录结构清晰（应用入口/配置/路由/数据访问层/测试）
    - 本地可启动并提供健康检查接口（如 `/health`）
    - Docker 镜像可构建，容器可启动

- [x] **任务2：定义并落盘数据库结构（SQLite + WAL）**
  - 优先级：高
  - 预估工作量：0.5-1天
  - 依赖关系：任务1
  - 验收标准：
    - SQLite 文件落盘到可配置路径（用于 NAS 卷挂载）
    - 启用 WAL 模式
    - 表结构包含：用户/会话或票据/邀请码/业务数据表/回收站标记/oplog/版本基线（用于截断）
    - 提供迁移/初始化方式（启动自动建表或迁移脚本）

### 第二阶段：认证与邀请注册（安全闭环）
- [x] **任务3：实现邀请注册与邀请码管理接口**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务2
  - 验收标准：
    - 管理员登录后可生成邀请码（一次性、7天有效）
    - 可查询邀请码状态（有效/已使用/已过期）
    - 可作废未使用邀请码
    - 无邀请码/无效邀请码无法注册

- [x] **任务4：实现登录/登出与 HttpOnly Cookie 登录态**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务2
  - 验收标准：
    - 登录成功返回并设置 Cookie（包含 HttpOnly 与 Secure）
    - 未登录访问受保护接口返回明确错误
    - 登出后 Cookie 失效，无法继续访问受保护接口
    - 写操作接口具备最小 CSRF 防护策略（与 Cookie 登录态匹配）

### 第三阶段：同步协议（增量 oplog）与幂等一致性
- [x] **任务5：制定并实现增量同步协议（oplog 拉取/上报）**
  - 优先级：高
  - 预估工作量：2-4天
  - 依赖关系：任务4
  - 验收标准：
    - 客户端可按版本号拉取增量变更（oplog）
    - 客户端可上报本地变更，服务端以“服务器接收时间”作为 LWW 裁决依据
    - 同一变更重复上报不产生重复数据（幂等）
    - 支持最小操作类型集合：新增/更新/删除（进回收站）/恢复/清空回收站

- [x] **任务6：实现 oplog 归档与截断机制（保留90天）**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务5
  - 验收标准：
    - 变更日志默认保留90天
    - 超期日志可合并为基线并截断
    - 很久未上线的客户端能被安全处理（按基线重建或触发全量重拉的等价路径）

### 第四阶段：回收站（仅交易记录）与一致性
- [x] **任务7：实现交易记录回收站（删除/恢复/清空）**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务5
  - 验收标准：
    - 删除交易记录进入回收站，正常列表不可见
    - 恢复后回到正常列表
    - 清空回收站后不可恢复，且通过同步在其他设备也不可恢复
    - 清空回收站具备二次确认所需的后端支持（接口幂等/错误码明确）

### 第五阶段：前端改造（与现有 v1.2 前端集成）
- [x] **任务8：新增登录/注册（邀请码）与登录态接入**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务4
  - 验收标准：
    - 未登录时只能访问登录/注册入口
    - 注册必须输入邀请码；成功后可登录
    - 登录后可正常使用记账核心功能

- [x] **任务9：实现设置页同步状态与“立即同步”按钮**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务5
  - 验收标准：
    - 设置页显示同步状态（已同步/同步中/失败/离线）
    - 点击“立即同步”可触发一次同步并反馈结果
    - 同步失败时提示原因（离线/鉴权过期/服务不可达等）

- [x] **任务10：实现自动后台同步（事件触发 + 失败指数退避）**
  - 优先级：高
  - 预估工作量：2-4天
  - 依赖关系：任务5、任务9
  - 验收标准：
    - 启动/回到前台/本地变更后触发同步
    - 同步失败指数退避，重试间隔上限为5分钟
    - 不会因失败导致持续高频请求影响 NAS 其他功能

### 第六阶段：部署（群晖 NAS + Cloudflare Tunnel）
- [x] **任务11：提供 docker compose 部署与数据落盘方案**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务1、任务2、任务4
  - 验收标准：
    - docker compose 一键启动后端与 cloudflared
    - SQLite 与日志目录落盘到 NAS 卷，容器重启不丢数据
    - 健康检查与基础日志可用于排障

- [ ] **任务12：完成 Cloudflare Tunnel 域名接入与 HTTPS 验证**
  - 优先级：高
  - 预估工作量：0.5-1天
  - 依赖关系：任务11
  - 验收标准：
    - 通过域名 HTTPS 访问可用
    - NAS 本机端口不对公网暴露（仅 Tunnel 入口）
    - CORS 仅允许前端域名

### 第七阶段：运维与质量保障
- [x] **任务13：日志滚动、保留与关键事件埋点**
  - 优先级：中
  - 预估工作量：0.5-1天
  - 依赖关系：任务1
  - 验收标准：
    - 日志按天滚动、保留7天并自动清理
    - 关键事件可定位：登录失败、同步失败、数据库异常

- [x] **任务14：数据体量告警与提示策略（10万条）**
  - 优先级：中
  - 预估工作量：0.5-1天
  - 依赖关系：任务9
  - 验收标准：
    - 达到10万条交易记录时，设置页出现提示与处理建议

- [x] **任务15：提供备份与恢复操作文档（运维级）**
  - 优先级：中
  - 预估工作量：0.5天
  - 依赖关系：任务11
  - 验收标准：
    - 文档包含：数据落盘位置、备份对象、恢复步骤
    - 使用群晖任务计划或等价方式可定期备份

- [ ] **任务16：端到端验收回归（按 v1.3 验收标准）**
  - 优先级：高
  - 预估工作量：1-2天
  - 依赖关系：任务3-15
  - 验收标准：
    - v1.3 验收标准 1-16 全部可复现通过
    - 回收站清空、邀请码一次性、同步退避等关键路径覆盖

## 进度跟踪
- 总体进度：87.5%
- 已完成：14/16
- 进行中：0/16
- 待开始：2/16
